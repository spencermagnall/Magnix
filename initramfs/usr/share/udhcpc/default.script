#!/bin/sh
case "$1" in
    bound|renew)
        ip addr add $ip/$mask dev $interface
        ip route add default via $router
        ;;
    deconfig)
        ip addr flush dev $interface
        ;;
esac
